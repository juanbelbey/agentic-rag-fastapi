# Dependencias del proyecto agentic-rag-fastapi
# Generado manualmente - solo paquetes directamente usados por el proyecto

# Agente y orquestacion
langchain==1.2.15
langchain-core==1.5.3
langchain-openai==1.2.1
langgraph==1.1.10

# LLM
openai==2.33.0

# API
fastapi==0.136.1
uvicorn==0.46.0

# Rate limiting (deploy publico en Render, stretch #2)
slowapi==0.1.9

# Validacion de datos
pydantic==2.12.4

# Observabilidad
langsmith==0.7.37

# Vectores (Capa 5A - indice en memoria)
numpy==2.4.6

# Postgres + pgvector (Capa 5B)
psycopg2-binary==2.9.11
pgvector==0.4.1

# Postgres checkpointer (Capa 5B.3) - psycopg v3 + pool, conviven con psycopg2 de arriba
langgraph-checkpoint-postgres==3.1.0
psycopg[binary]==3.3.4
psycopg-pool==3.3.1

# Extraccion de texto de PDFs (Capa 5B.4)
pypdf==5.1.0

# Configuracion
python-dotenv==1.2.1

# Tests
pytest==9.0.3
pytest-html==4.2.0

# Evals de generacion RAG (stretch RAGAS)
ragas==0.4.3
# Pin explicito, no es dependencia directa del proyecto: ragas no fija version de
# langchain-community y la ultima (0.4.2) borro langchain_community/chat_models/vertexai.py,
# que ragas/llms/base.py importa sin condicion al cargar el paquete (ImportError inmediato).
# 0.4.1 todavia lo tiene y pide langchain-core>=1.0.1 (compatible con el 1.5.3 de arriba).
# Encontrado y verificado 2026-08-05 - el test de compatibilidad del 2026-08-04 en venv
# aislado no lo detecto.
langchain-community==0.4.1
